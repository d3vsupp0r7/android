<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.lba.android.simple.trainer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.dynamictable.DynamicTableExample1SingleRowActivity"></activity>
        <activity android:name=".activity.layout.tablelayout.TableLayoutExample2Activity" />
        <activity android:name=".activity.layout.tablelayout.TableLayoutExample1Activity" />
        <activity android:name=".activity.LayoutSampleIndexActivity" />
        <activity android:name=".activity.database.DatabaseCRUDActivity" />
        <activity android:name=".activity.DatabaseSampleIndexActivity" />
        <activity android:name=".activity.DateTimeSampleIndexActivity" />
        <activity android:name=".activity.MasterDetailSampleIndexActivity" />
        <activity android:name=".activity.DynamicTableSampleIndexActivity" />
        <activity android:name=".activity.datetime.picker.DateAndTimePickerActivity" />
        <activity
            android:name=".activity.masterdetail.items.ItemListActivity"
            android:label="@string/title_item_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.masterdetail.items.ItemDetailActivity"
            android:label="@string/title_item_detail"
            android:parentActivityName=".activity.masterdetail.items.ItemListActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="org.lba.android.simple.trainer.activity.masterdetail.items.ItemListActivity" />
        </activity>
        <activity
            android:name=".activity.layout.linear.LinearLayoutHorizontalActivity"
            android:label="@string/title_activity_linear_layout_horizontal"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.layout.linear.LinearLayoutVerticalActivity"
            android:label="@string/title_activity_linear_layout"
            android:theme="@style/AppTheme.NoActionBar" /> <!-- Main activity -->
        <activity android:name=".MainActivity">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->

        </activity> <!-- Second Activity -->
        <activity
            android:name=".SecondActivity"
            android:label="@string/app_name" /> <!-- DynamicTable Activity -->
        <activity
            android:name=".activity.dynamictable.DynamicTableActivity"
            android:label="@string/app_name" /> <!-- SplashScreen activity -->
        <activity
            android:name=".SplashActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>